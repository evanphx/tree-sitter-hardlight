import fmt

1 + 3 * 2 + 2
fmt.print("hello %s %d, %s, 0.1 + 0.2 = %f, %d\n", "hardlight", 3 + 4, 1/3, 0.1 + 0.2, 1 + 3 * 2 + 2)
fmt.print("hello js: 0.1 + 0.2 == 0.3 => %s\n", 0.1 + 0.2 == 0.3)

priv let x = => {
  fmt.print("capture\n")
}

x()
